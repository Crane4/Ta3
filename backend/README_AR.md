# خادم سامر - SAMER Backend

خادم باكند لاستقبال وعرض الحوادث من تطبيق سامر.

## التثبيت

```bash
cd backend
npm install
```

## تشغيل الخادم

```bash
npm start
```

سيعمل الخادم على: `http://localhost:3000`

## لوحة التحكم

افتح المتصفح على: `http://localhost:3000` لعرض لوحة التحكم

لوحة التحكم تعرض:
- 📊 إحصائيات الحوادث
- 📱 قائمة بجميع الحوادث المستلمة
- 🔄 تحديث تلقائي كل 3 ثواني

## نقاط API

### POST /api/incidents
استقبال حادث من التطبيق

### GET /api/incidents
الحصول على جميع الحوادث

### GET /api/stats
الحصول على إحصائيات الحوادث

## ملاحظات

- البيانات حالياً مخزنة في الذاكرة (Memory)
- للاستخدام في الإنتاج، استخدم قاعدة بيانات
- تأكد من إضافة الأمان والمصادقة في الإنتاج

